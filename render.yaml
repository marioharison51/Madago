services:
  - type: web
    name: madago-backend
    env: node
    plan: free
    rootDir: back-end
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false
